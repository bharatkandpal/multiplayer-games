// @mpg/server — authoritative backend (HTTP + Socket.IO).
// Real server, room manager, and API land in MPG-011+.
// Stub only, to prove the workspace links against @mpg/engine (MPG-001).

import { ENGINE_VERSION } from "@mpg/engine";

console.log(`@mpg/server stub — linked @mpg/engine v${ENGINE_VERSION}`);

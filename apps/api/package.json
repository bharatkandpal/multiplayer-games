{
  "name": "@mpg/api",
  "version": "0.0.0",
  "private": true,
  "description": "Serverless HTTP API (Vercel functions): the stateless endpoints (sessions, leaderboard, results, share, events) from @mpg/server, backed by Neon. Rooms + Socket.IO stay on the long-running @mpg/server container.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:fast": "vitest run",
    "dev": "vercel dev",
    "build": "node scripts/bundle.mjs"
  },
  "dependencies": {
    "@mpg/engine": "workspace:*",
    "@mpg/server": "workspace:*",
    "@resvg/resvg-js": "^2.6.2"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "esbuild": "^0.28.2"
  }
}

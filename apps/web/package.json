{
  "name": "@mpg/web",
  "version": "0.0.0",
  "private": true,
  "description": "React client: design system, board renderers, seat-config UI, optimistic-UI net layer.",
  "type": "module",
  "scripts": {
    "build": "echo \"[@mpg/web] Vite build configured in MPG-009\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "dev": "echo \"[@mpg/web] Vite dev server configured in MPG-009\""
  },
  "dependencies": {
    "@mpg/engine": "workspace:*"
  }
}

// @mpg/web — React client entry (stub).
// Real Vite + React app, design system, and screens land in MPG-002 / MPG-009.
// Stub only, to prove the workspace links against @mpg/engine (MPG-001).

import { ENGINE_VERSION } from "@mpg/engine";

console.log(`@mpg/web stub — linked @mpg/engine v${ENGINE_VERSION}`);

# TODO

## CI / Deploy

- [x] **Add Vercel deploy secrets** (GitHub → Settings → Secrets and variables → Actions).
      `VERCEL_TOKEN` (team-scoped), `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` — all set and verified.
- [x] Confirm the Vercel project **Root Directory** is `apps/web` (where `vercel.json` lives).
- [x] Deploy pipeline verified end-to-end on PR #1: preview deploy + PR comment working.
